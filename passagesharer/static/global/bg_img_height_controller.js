document.addEventListener('DOMContentLoaded', () => {
    const bg_img_viewer = document.querySelector('.bg_img_viewer');
    if (bg_img_viewer) {
        bg_img_viewer.style.height = `${window.innerHeight}px`;
    };
});
